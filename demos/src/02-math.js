function add(a, b) {
  return a + b;
}

function multiply(a, b) {
  return a * b;
}

function divide(a, b) {
  return a / b;
}

function greetings(firstName) {
  return `Hello ${firstName}`;
}

module.exports = {
  add,
  multiply,
  divide,
  greetings,
}
